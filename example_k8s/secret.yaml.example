# Template only — create the real secret out-of-band (never commit a real token):
#   kubectl -n llm-sandbox create secret generic llm-sandbox --from-literal=token=$(openssl rand -hex 32)
apiVersion: v1
kind: Secret
metadata:
  name: llm-sandbox
  namespace: llm-sandbox
type: Opaque
stringData:
  token: CHANGE-ME
